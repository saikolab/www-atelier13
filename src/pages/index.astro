---
import { DEFAULT_LOCALE } from "@/i18n/config";
const toPath = `/${DEFAULT_LOCALE}/`;
const to = new URL(toPath, Astro.url); // ensures correct base handling
Astro.redirect(to.pathname, 307);
---
